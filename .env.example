# Tile CDN base URL (no trailing slash).
# Leave unset during local dev to serve from public/tiles/.
# For production, point at your Cloudflare R2 public bucket:
#   NEXT_PUBLIC_TILE_BASE=https://tiles.your-r2-subdomain.r2.dev
NEXT_PUBLIC_TILE_BASE=
